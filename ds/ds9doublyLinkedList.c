/*
 *	         DS Lab
 *	   CSE Sec 2, Group 5
 *	      Nov 7, 2012
 *	       UE 113090
 *  AIM: Word Processor using multiple doubly linked list
 */

/*
#include<stdio.h>
#include<stdlib.h>
struct nodeS {
    char info;
    struct nodeS *next;
    struct nodeS *prev;
    struct nodeS *up;
    struct nodeS *down;    //next points to var of type node
};
typedef struct nodeS *NODEPTR;
//********Global Variable*******
//********Functions Prototypes**********

//*********Main Function**************
int main(){
    NODEPTR list,temp,lst,ptr;
    char input;
    list=(NODEPTR)malloc(sizeof(struct nodeS));
    list->next=NULL;
    list->prev=NULL;
    list->up=NULL;
    list->down=NULL;   
    temp=start;
    scanf("%c",&input);
    if(input=='\n'){
        lst=(NODEPTR)malloc(sizeof(struct nodeS));
        ptr=lst;
        }
    else if (input!='\0'||input!='\n'){
        lst=(NODEPTR)malloc(sizeof(struct nodeS));
        lst->info=input;
        temp->next=lst;
        lst->next=NULL;
        lst->prev=list;
        lst->up=NULL;
        lst->down=NULL;
        temp=lst;
    }
    
}

*/

